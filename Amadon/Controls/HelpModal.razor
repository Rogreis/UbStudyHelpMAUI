@using Amadon.Controls.Help


<style>

    .helpButton {
        font-size: 70%; /* decrease the font size to 80% of the current size */
    }

    .centered-bar {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100%; /* or any height you want */
    }

    .imageFull {
        width: 430px;
        height: 300px;
    }

    .imageSmall {
        width: 300px;
        height: 200px;
    }

    .imageMenubar {
        width: 430px;
        height: 110px;
    }


    table, th, td {
        border: 0px;
    }

    h3, h4 {
        color: gold;
    }


  /*  .modal-dark-mode {
        background-color: #1a1a2e;
        color: #f2f2f2;
    }

    .modal-dark-mode .modal-content {
        background-color: #1a1a2e;
        color: #f2f2f2;
    }

    .modal-dark-mode .modal-header, .modal-dark-mode .modal-body, .modal-dark-mode .modal-footer {
        border-color: #30304d;
    }
*/


</style>

<div class="modal" id="myModal">
    <div class="modal-dialog modal-lg  modal-dialog-centered">
        <div class="modal-content">

            <!-- Modal Header -->
            <div class="modal-header">
                <h4 class="modal-title">Amadon Help</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>

            <!-- Modal body -->
            <div class="modal-body">
 @*               <SettingsHelp></SettingsHelp>
 *@           </div>

            <!-- Modal footer -->
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Close</button>
            </div>

        </div>
    </div>
</div>


@*
<Modal @ref="modalRef" Class="@(IsDarkMode ? "modal-dark-mode" : "")">
    <ModalContent Scrollable Centered Margin="Margin.Is5.FromTop">
        <ModalHeader>
            <ModalTitle>Employee edit</ModalTitle>
            <CloseButton />
        </ModalHeader>
        <ModalBody MaxHeight=500>
            <SettingsHelp></SettingsHelp>
        </ModalBody>
        <ModalFooter>
            <Button Color="Color.Secondary" Clicked="@HideModal">Close</Button>
            <Button Color="Color.Primary" Clicked="@HideModal">Save Changes</Button>
        </ModalFooter>
    </ModalContent>
</Modal>
*@
@code {
    // reference to the modal component
    private Modal modalRef;
    public bool IsDarkMode { get; set; } = true;

    public Task ShowModal()
    {
        return modalRef.Show();
    }

    public Task HideModal()
    {
        return modalRef.Hide();
    }
}

@code {

}
